import styled from "styled-components";

export const SearchBarModalContainer = styled.div`
	position:relative;
	z-index:5;
`;

export const SearchBarList = styled.div`
	position:absolute;
	width:100%;
	background:rgba(16, 16, 16, .9);
`;
